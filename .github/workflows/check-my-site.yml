name: CI Pipeline

on:
  push:
    paths:
      - index.html
  pull_request:
    paths:
      - index.html

jobs:
  kill-ci:
    runs-on: ubuntu-latest
    steps:
      - name: Убийство CI
        run: |
          echo "💥 Принудительный сбой. CI остановлен."
          exit 1
