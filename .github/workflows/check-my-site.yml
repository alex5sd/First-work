name: CI Pipeline

on:
  push:
    paths:
      - index.html
  pull_request:
    paths:
      - index.html

jobs:
  force-fail-ci:
    runs-on: ubuntu-latest
    steps:
      - name: Информация о запуске
        run: |
          echo "🔄 CI запущен. Сейчас будет принудительный сбой."

      - name: ❌ Принудительный провал CI
        run: |
          echo "🔥 Этот CI намеренно завершается с ошибкой."
          echo "🚫 Разработка остановлена. CD не будет запущен."
          exit 1
