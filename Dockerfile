# Шаг 1: Берем "фундамент" — готовый веб-сервер Nginx.
# Он очень легкий и умеет показывать HTML-страницы всему миру.
FROM nginx:alpine

# Шаг 2: Копируем твой файл с Домбаем внутрь этого сервера.
# Мы кладем его в специальную папку, откуда Nginx забирает файлы для показа.
COPY index.html /usr/share/nginx/html/index.html

# Все! Больше ничего не нужно. Контейнер готов к работе.
